import {View, Text, StyleSheet } from "react-native";

const JoinSession = () =>
    <View style={styles.container}>
        <Text>Page Join Session</Text>
    </View>

export default JoinSession;

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
});